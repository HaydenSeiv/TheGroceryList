import React from "react";

export default function Root(){
    return (
        <>
            <h1>This would be the login page</h1>
            <a href={`/list/`}>Click for Main List</a>
        </>
    );
}